<div class="max-w-500px mx-auto">
  <slot />
</div>

<style global>
  /* :root {
    font-size: 24px;
  }
  body {
    @apply p-2;
  }
  input {
    @apply h-8 rounded-lg bg-light-blue-200;
  }
  button {
    @apply h-8 rounded-lg;
  }
  select {
    @apply rounded-lg bg-light-blue-200;
  } */

  .a {
    @apply bg-gray-500;
  }
  .b {
    @apply bg-fuchsia-800;
  }
  .c {
    @apply bg-purple-800;
  }
  .d {
    @apply bg-teal-800;
  }
  .e {
    @apply bg-lime-800;
  }
  .f {
    @apply bg-yellow-800;
  }
  .g {
    @apply bg-rose-800;
  }
  .h {
    @apply bg-zinc-800;
  }
</style>
